# tokens.example.txt — one room_token per line for batch stage-2 bundling.
# Copy this to tokens.txt (gitignored), edit with your real tokens, then:
#   python _deploy_stage2.py --from-file tokens.txt
# or
#   .\deploy_to_vps.ps1 -Vps root@1.2.3.4 -TokensFile tokens.txt
#
# Lines starting with '#' are comments and ignored.
# Blank lines are ignored.
# Tokens can be any string (that's what the host uses in host_config.json).

# Sample entries — replace with your real tokens:
# ABC-EXAMPLE-TOKEN-1
# XYZ-EXAMPLE-TOKEN-2
# test-deploy-token
